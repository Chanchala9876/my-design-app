<!DOCTYPE html>
<html>
<head>
  <title><%= category %> Stores | DesignHub</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      background-attachment: fixed;
      background-size: cover;
    }
    .glass-effect {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    .store-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      transition: all 0.3s ease;
    }
    .store-card:hover {
      transform: translateY(-5px) scale(1.02);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }
    .hero-bg {
      background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), 
                  url('https://images.unsplash.com/photo-1441986300917-64674bd600d8?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
    }
  </style>
</head>
<body class="text-white">

  <nav class="glass-effect p-6 flex justify-between items-center fixed w-full z-50">
    <div class="flex items-center space-x-2">
      <i class="fas fa-palette text-2xl text-purple-400"></i>
      <h1 class="text-2xl font-bold bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent">DesignHub</h1>
    </div>
    <a href="/content" class="hover:text-purple-300 transition-colors duration-300">
      <i class="fas fa-arrow-left mr-2"></i>Back to Categories
    </a>
  </nav>

  <header class="hero-bg text-center py-24 pt-32">
    <div class="max-w-4xl mx-auto px-6">
      <h2 class="text-5xl font-bold mb-4 bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent">
        <i class="fas fa-store mr-4"></i><%= category %> Designer Stores
      </h2>
      <p class="text-xl text-gray-200 mb-8">Explore talented student designers in the <%= category %> category</p>
      <div class="bg-white bg-opacity-20 px-6 py-3 rounded-full inline-block">
        <i class="fas fa-users mr-2"></i>
        <span><%= designers.length %> Designer<%= designers.length !== 1 ? 's' : '' %></span>
      </div>
    </div>
  </header>

  <main class="py-16 px-6">
    <div class="max-w-7xl mx-auto">
      <% if (error) { %>
        <div class="text-center py-16">
          <i class="fas fa-exclamation-circle text-6xl text-red-400 mb-4"></i>
          <p class="text-2xl text-gray-300 mb-2"><%= error %></p>
          <p class="text-gray-400">Please try again or contact support if the problem persists.</p>
        </div>
      <% } else if (designers.length === 0) { %>
        <div class="text-center py-16">
          <i class="fas fa-search text-6xl text-gray-400 mb-4"></i>
          <p class="text-2xl text-gray-300 mb-2">No designers found</p>
          <p class="text-gray-400">No designers are currently available in this category.</p>
        </div>
      <% } else { %>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
          <% designers.forEach(designer => { %>
            <div class="store-card rounded-2xl shadow-xl overflow-hidden">
              <div class="p-6 text-center">
                <div class="w-20 h-20 bg-gradient-to-r from-purple-400 to-pink-400 rounded-full mx-auto mb-4 flex items-center justify-center">
                  <i class="fas fa-store text-2xl text-white"></i>
                </div>
                <h3 class="text-xl font-bold text-gray-800 mb-2"><%= designer.storeName %></h3>
                <p class="text-gray-600 mb-1">
                  <i class="fas fa-user mr-2"></i><%= designer.name %>
                </p>
                <p class="text-gray-500 text-sm mb-6">
                  <i class="fas fa-envelope mr-2"></i><%= designer.email %>
                </p>
                <a href="/store/<%= designer._id %>" class="bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white px-6 py-3 rounded-full font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg inline-block">
                  <i class="fas fa-eye mr-2"></i>View Store
                </a>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </main>

  <footer class="py-12 text-center">
    <div class="glass-effect p-8 rounded-2xl max-w-4xl mx-auto">
      <div class="flex justify-center space-x-6 mb-6">
        <a href="#" class="text-2xl hover:text-purple-400 transition-colors duration-300">
          <i class="fab fa-facebook"></i>
        </a>
        <a href="#" class="text-2xl hover:text-purple-400 transition-colors duration-300">
          <i class="fab fa-twitter"></i>
        </a>
        <a href="#" class="text-2xl hover:text-purple-400 transition-colors duration-300">
          <i class="fab fa-instagram"></i>
        </a>
        <a href="#" class="text-2xl hover:text-purple-400 transition-colors duration-300">
          <i class="fab fa-linkedin"></i>
        </a>
      </div>
      <p class="text-gray-300">&copy; 2025 DesignHub. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
