<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful - DesignHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .glass-effect {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-4">
    <div class="glass-effect rounded-2xl p-8 max-w-md w-full text-center">
        <!-- Success Icon -->
        <div class="mb-6">
            <div class="w-20 h-20 bg-green-500 rounded-full flex items-center justify-center mx-auto mb-4">
                <i class="fas fa-check text-white text-3xl"></i>
            </div>
        </div>

        <!-- Success Message -->
        <h1 class="text-3xl font-bold text-white mb-4">Payment Successful!</h1>
        <p class="text-gray-200 mb-6">
            Your order has been placed successfully. You will receive a confirmation email shortly.
        </p>

        <!-- Order Details -->
        <div class="bg-white bg-opacity-10 rounded-lg p-4 mb-6">
            <h3 class="text-white font-semibold mb-2">Order Details</h3>
            <p class="text-gray-200 text-sm">
                Order ID: <span class="font-mono"><%= orderId || 'N/A' %></span>
            </p>
            <p class="text-gray-200 text-sm">
                Payment ID: <span class="font-mono"><%= paymentId || 'N/A' %></span>
            </p>
        </div>

        <!-- Next Steps -->
        <div class="mb-6">
            <h3 class="text-white font-semibold mb-3">What's Next?</h3>
            <div class="space-y-2 text-left">
                <div class="flex items-center text-gray-200 text-sm">
                    <i class="fas fa-envelope text-purple-300 mr-2"></i>
                    <span>Confirmation email sent</span>
                </div>
                <div class="flex items-center text-gray-200 text-sm">
                    <i class="fas fa-box text-purple-300 mr-2"></i>
                    <span>Order being processed</span>
                </div>
                <div class="flex items-center text-gray-200 text-sm">
                    <i class="fas fa-truck text-purple-300 mr-2"></i>
                    <span>Shipping updates via email</span>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="space-y-3">
            <a href="/user/dashboard" 
               class="block w-full bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white py-3 px-6 rounded-lg font-semibold transition-all duration-300 transform hover:scale-105">
                <i class="fas fa-user mr-2"></i>View My Orders
            </a>
            
            <a href="/content" 
               class="block w-full bg-white bg-opacity-20 hover:bg-opacity-30 text-white py-3 px-6 rounded-lg font-semibold transition-all duration-300">
                <i class="fas fa-shopping-bag mr-2"></i>Continue Shopping
            </a>
        </div>

        <!-- Support Info -->
        <div class="mt-6 pt-6 border-t border-white border-opacity-20">
            <p class="text-gray-300 text-sm mb-2">Need help?</p>
            <p class="text-gray-300 text-xs">
                Contact us at <a href="mailto:support@designhub.com" class="text-purple-300 hover:underline">support@designhub.com</a>
            </p>
        </div>
    </div>

    <script>
        // Auto-redirect to dashboard after 10 seconds
        setTimeout(() => {
            window.location.href = '/user/dashboard';
        }, 10000);
    </script>
</body>
</html> 