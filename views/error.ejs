<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Error | DesignHub</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      background-attachment: fixed;
      background-size: cover;
    }
    .glass-effect {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
  </style>
</head>
<body class="text-white min-h-screen flex flex-col">
  <!-- Navbar -->
  <nav class="glass-effect px-6 py-4 flex justify-between items-center shadow-lg">
    <div class="flex items-center space-x-2">
      <i class="fas fa-palette text-2xl text-purple-400"></i>
      <a href="/" class="text-2xl font-bold bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent">DesignHub</a>
    </div>
    <a href="/content" class="hover:text-purple-300 transition-colors duration-300">
      <i class="fas fa-arrow-left mr-2"></i>Back to Categories
    </a>
  </nav>

  <!-- Error Content -->
  <main class="flex-grow flex items-center justify-center p-6">
    <div class="glass-effect p-8 rounded-2xl max-w-lg w-full text-center">
      <i class="fas fa-exclamation-circle text-6xl text-red-400 mb-6"></i>
      <h1 class="text-3xl font-bold mb-4"><%= error %></h1>
      <p class="text-gray-300 mb-8"><%= message %></p>
      <div class="flex justify-center space-x-4">
        <a href="javascript:history.back()" class="bg-gradient-to-r from-purple-500 to-pink-500 text-white px-6 py-3 rounded-full font-bold shadow-lg hover:from-purple-600 hover:to-pink-600 transition-all duration-300">
          <i class="fas fa-arrow-left mr-2"></i>Go Back
        </a>
        <a href="/content" class="bg-gradient-to-r from-blue-500 to-indigo-500 text-white px-6 py-3 rounded-full font-bold shadow-lg hover:from-blue-600 hover:to-indigo-600 transition-all duration-300">
          <i class="fas fa-home mr-2"></i>Home
        </a>
      </div>
    </div>
  </main>
</body>
</html>
